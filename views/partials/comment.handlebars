<div class="container">
    <div class="row bg-dark p-3 rounded">
        {{>upvote}}
        <div class="col-11">
            <p class="lead">{{user.username}}</p>
            <p>{{body}}</p>
        </div>
    </div>
</div>